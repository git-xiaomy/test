<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.scaffold.mapper.SysRoleMapper">
    
    <select id="findRolesByUserId" resultType="com.example.scaffold.entity.SysRole">
        SELECT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
        AND r.deleted = 0 AND r.status = 1
    </select>
    
    <select id="findRolesByUserIds" resultType="com.example.scaffold.entity.SysRole">
        SELECT DISTINCT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
        AND r.deleted = 0 AND r.status = 1
    </select>
    
</mapper>